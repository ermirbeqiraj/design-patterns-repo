namespace PatternCollection.ObjectAdapter;

// The old timer here has just way too many references in an old project, not worth changing at this time.
public class OldFrameworkService
{
    public string Method1()
    {
        return nameof(Method1);
    }

    public string Method2()
    {
        return nameof(Method2);
    }
}
